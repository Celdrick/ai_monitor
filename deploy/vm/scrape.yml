global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: ai-monitor-agent
    file_sd_configs:
      - files:
          - /file_sd/agents.json
        refresh_interval: 30s
